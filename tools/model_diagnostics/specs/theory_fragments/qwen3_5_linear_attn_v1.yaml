fragment_id: qwen3_5_linear_attn_v1
fragment_kind: model_decoder
# Qwen3-Next fused linear attention (one op).
stages:
  - id: linear_attention
    modules:
      - name: linear_attention
        tensors:
          INPUT[0]: {shape: "[T, H]", dtype: ACT}
          OUTPUT[0]: {shape: "[T, H]", dtype: OUT}
    runtime:
      boundary_operators: [linear_attention]
      ignored_operators:
        - view
        - permute
        - mul
        - add
        - convert_element_type
        - mean
        - pow
        - rsqrt
        - expand
        - clone
        - neg
        - sigmoid
        - select
        - cat
        - slice
        - index
        - split
        - unsqueeze
        - copy_
        - dynamic_quantize_symmetric
        - linear_attn_causal_conv
